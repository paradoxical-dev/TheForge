# Custom Dots For end-4

This folder contains the customized dotfiles to replace/overwrite the default configs.

> [!NOTE]
> In the future (if I stick with end-4) this will be automatically applied from the end-4 install scirpt.
> For now, they are just static files I'll need to manually copy on a fresh install.